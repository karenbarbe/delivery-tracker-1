<div>
  <h1>
    Delivery Tracker
  </h1>

  <p>Know if something gets lost in the mail.</p>
</div>


<hr>

<div>
  <div>
    <h2>
      Expecting a package?
    </h2>

    <form action="/insert_delivery" method="post">

      <div>
        <label for="description_box">
          Description
        </label>

        <input type="text" id="description_box" name="query_description">
      </div>

      <div>
        <label for="supposed_to_arrive_on_box">
          Supposed to arrive on
        </label>

        <input type="date" id="supposed_to_arrive_on_box" name="query_supposed_to_arrive_on">
      </div>

          
      <div>
        <label for="details_box">
          Details
        </label>

        <textarea id="details_box" name="query_details" rows="3"></textarea>
      </div>

      <button>
        Log delivery
      </button>
    </form>
  </div>
</div>

<div class="waiting-on">
  <h2>Waiting on</h2>
  <ul>
    <% @waiting.each do |a_delivery| %>
    <li>
      <p><%= a_delivery.description %></p>
      <p style="color: red">Supposed to arrive on <%= a_delivery.supposed_to_arrive_on %></p>
      <p><%= a_delivery.details %></p> 
      <div>
        <form action="/modify_delivery/<%= a_delivery.id %>" method="post">
          <input type="hidden" name="arrived" value="true">
          <button>Mark as received</button>
        </form>
      </div>

    </li>
    <% end %>
  </ul>
</div>

<div class="received">
  <h2>Received</h2>
  <ul>
    <% @received.each do |a_delivery| %>
  <li>
    <p><%= a_delivery.description %></p>
    <p style="color: red">Supposed to arrive on <%= a_delivery.supposed_to_arrive_on %></p>
    <p><%= a_delivery.details %></p>
    <div>
      <form action="/modify_delivery/<%= a_delivery.id %>" method="post">
        <input type="hidden" name="arrived" value="true">
        <button>Mark as received</button>
      </form>
    </div>

  </li>
<% end %>

  </ul>
</div>
